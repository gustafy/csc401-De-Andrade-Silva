<div class="hero-unit">
            	<h1>Photo Art</h1>
            	<p>Are you an amateur photographer? Do you want to publish your work?<br> Here is the right place? Photo art: A website where you can share your work and know what people think about it. Upload now your pictures!!</p>
            	<p>
                    <a class="btn btn-primary btn-large">
                        Get started
                    </a>
            	</p>
         	</div>


<% @users.each do |user| %>

	<h2><%= link_to"#{user.name}", user_url(user) %></h2>
    <p><%= user.about %></p>
    <div class="picture-capsule">
 
        <% user.imgs.each do |img| %>
        <%= image_tag img.pic.url(:medium) %>
        <% end %>
   
    </div>  
	
	<p><%= link_to "Edit", edit_user_url(user) %> -

	 <%= link_to "delete", user, :method => :destroy,
	 data: {confirm:"delete user?"} %></p>

<% end %>

<hr />


<%= link_to "Add New User", new_user_url %>

<hr />

<h1>Listing users</h1>
 <p id="notice"><%= notice %></p>
<table>
<tr>
<th>Username</th>
<th>Email</th>
<th></th>
<th></th>
<th></th>
</tr>
 
<% @users.each do |user| %>
<tr>
<td><%= user.name %></td>
<td><%= user.username %></td>
<td><%= user.email %></td>
<td><%= link_to 'Show', user %></td>
<td><%= link_to 'Edit', edit_user_path(user) %></td>
<td><%= link_to 'Destroy', user, :confirm => 'Are you sure?', :method => :delete %></td>
</tr>
<% end %>
</table>
 
<br />
 
<%= link_to 'New User', new_user_path %>